version: "3.6"

services:
  postgres:
    image: "nikolanna/custom-postgres:latest"
    ports:
      - "5432:5432"      
    networks:
      - persistence

networks:
  persistence: {}

volumes:
  postgres-data: {}
